import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RegisterDriverCompletedPage } from './register-driver-completed';

@NgModule({
  declarations: [
    RegisterDriverCompletedPage,
  ],
  imports: [
    IonicPageModule.forChild(RegisterDriverCompletedPage),
  ],
})
export class RegisterDriverCompletedPageModule {}
